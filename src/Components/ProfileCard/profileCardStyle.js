import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container : {
        borderWidth : 1,
        margin : 7,
        padding : 7,
        justifyContent : "center",
        alignItems : "center",
        borderColor : "gray"
    },

    image : {
        width : 100,
        height : 100,
        // borderWidth : 15
        borderRadius : 56
    }
})