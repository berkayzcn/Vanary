import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container : {
        // borderWidth : 1,
        borderRadius : 10,
        //padding : 5,
        //margin : 4,
       // padding : 14

        // flex : 1
    },

    innerContainer : {
        margin : 24
    }
})